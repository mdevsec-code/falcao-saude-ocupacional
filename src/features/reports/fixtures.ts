import type { ReportRecord } from './types';

/**
 * Fixtures para o módulo Relatórios. Substituiremos por uma query
 * ao MSW (e depois à API) assim que o backend estiver disponível.
 * Sem registros pré-cadastrados — os relatórios refletem os dados reais lançados após o lançamento.
 */
export const REPORT_FIXTURES: readonly ReportRecord[] = [];
